interface:
  display_name: "Evolver"
  short_description: "Collect session traces for principle distillation"
  default_prompt: "Use $evolver to capture structured traces from this session for future improvement."
