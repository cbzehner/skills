interface:
  display_name: "Seance"
  short_description: "Search past sessions, transcripts, and logs"
  default_prompt: "Use $seance to find the relevant past session logs and reconstruct what happened."
